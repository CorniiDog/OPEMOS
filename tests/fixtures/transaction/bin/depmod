#!/usr/bin/env bash
set -euo pipefail

printf 'depmod %s\n' "$*" >> "${MOCK_COMMAND_LOG:?}"

if [[ "${MOCK_FAIL_POINT:-}" == "depmod" &&
      ! -e "${MOCK_FAILURE_MARKER:?}" ]]; then
    touch "$MOCK_FAILURE_MARKER"
    printf 'Injected depmod failure.\n' >&2
    exit 86
fi

exit 0
